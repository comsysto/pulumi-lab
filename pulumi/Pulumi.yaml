name: fargate-deployment
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Pulumi Lab Deployment
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
